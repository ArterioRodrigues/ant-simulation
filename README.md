### How to run 

The commands to build and run this project is inside of `run.sh` file
Use the bellow commands to run this projects.

```sh
mkdir build

chmod +x run.sh
./run.sh
```

**important** - install python3, pip3 and numpy and matplotlib. Else remvoe the matplotlib-cpp header file.
